server {
    server {
        server_name localost;
        root /var;
        listen 80;
        index file;
        location /path {
            root g;
            index file.html;
        }
    }
}